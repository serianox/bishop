language: node_js
sudo: false
node_js:
  - "node"
script:
  - git config --global gpg.program gpg2
  - git verify-commit `git rev-parse --verify HEAD`
  - yarn run bs ci
cache:
  yarn : true
  directories:
    - "node_modules"
